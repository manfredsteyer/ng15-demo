import { AvoidDirective } from './avoid.directive';

describe('AvoidDirective', () => {
  it('should create an instance', () => {
    const directive = new AvoidDirective();
    expect(directive).toBeTruthy();
  });
});
